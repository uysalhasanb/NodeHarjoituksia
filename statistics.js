// A CJS LIBRARY TO CALCULATE BASIC STATISTICAL CHARASTERISTICS OF AN ARRAY
// ========================================================================

// LIBRARIES
// ---------

// Statistical operations based on math.js library
// It is not built-in Math class, need to install this 1st
const mathjs = require('mathjs');

// A class to calculate statistical charasteristics at given precision
class ArrayStats{
    constructor(array, decimals){
        this.array = array;
        this.decimals = decimals;
    }


    //Average
}